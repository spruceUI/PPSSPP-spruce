name: Build PPSSPPSDL_SmartProS

on:
  workflow_dispatch:
    inputs:
      ppsspp_version:
        description: 'PPSSPP version tag'
        default: 'v1.20.3'
  workflow_call:
    inputs:
      ppsspp_version:
        type: string
        default: 'v1.20.3'

permissions:
  contents: write
  packages: write

env:
  IMAGE: ghcr.io/spruceui/ppsspp-builder-tsps

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ensure latest release exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="latest"
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" \
              --target "${{ github.ref_name }}" \
              --title "Latest Build" \
              --notes "PPSSPP ${{ github.event.inputs.ppsspp_version || 'v1.20.3' }} binaries for SpruceOS" \
            || true

      - name: Download SDK tarball from release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p toolchains/smartpro_s
          gh release download sdk-toolchains \
            -p "sdk_tg5050_linux_v1.0.0.tgz" \
            -D toolchains/smartpro_s/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.tsps
          push: true
          tags: ${{ env.IMAGE }}:latest
          cache-from: type=registry,ref=${{ env.IMAGE }}:cache
          cache-to: type=registry,ref=${{ env.IMAGE }}:cache,mode=max

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ccache
          key: ccache-tsps-${{ github.event.inputs.ppsspp_version || 'v1.20.3' }}-${{ github.sha }}
          restore-keys: |
            ccache-tsps-${{ github.event.inputs.ppsspp_version || 'v1.20.3' }}-
            ccache-tsps-

      - name: Build PPSSPP
        run: |
          mkdir -p output ccache
          docker run --rm \
            -e PPSSPP_VERSION=${{ github.event.inputs.ppsspp_version || 'v1.20.3' }} \
            -v ${{ github.workspace }}/output:/output \
            -v ${{ github.workspace }}/ccache:/ccache \
            ${{ env.IMAGE }}:latest

      - name: Upload binary and assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "latest" output/PPSSPPSDL_SmartProS --clobber
